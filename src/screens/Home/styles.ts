import styled from 'styled-components/native';

export const Container = styled.View({
  flex: 1,
  backgroundColor: '#FFFFFF',
});

export const ScrollPost = styled.ScrollView({
  flex: 1,
  backgroundColor: '#FFFFFF',
});

export const WrapperTitle = styled.View({
  justifyContent: 'center',
  alignItems: 'center',
  marginTop: 40,
});
